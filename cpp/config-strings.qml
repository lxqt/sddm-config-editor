ListModel {
    ListElement {
      name: QT_TR_NOOP("Autologin")
    }
    ListElement {
      name: QT_TR_NOOP("Autologin again on session exit")
    }
    ListElement {
      name: QT_TR_NOOP("Autologin session")
    }
    ListElement {
      name: QT_TR_NOOP("Autologin user")
    }
    ListElement {
      name: QT_TR_NOOP("General")
    }
    ListElement {
      name: QT_TR_NOOP("Halt command")
    }
    ListElement {
      name: QT_TR_NOOP("Initial NumLock state")
    }
    ListElement {
      name: QT_TR_NOOP("Valid values: on|off|none
If property is set to none, numlock won't be changed")
    }
    ListElement {
      name: QT_TR_NOOP("Reboot command")
    }
    ListElement {
      name: QT_TR_NOOP("Theme")
    }
    ListElement {
      name: QT_TR_NOOP("Current theme name")
    }
    ListElement {
      name: QT_TR_NOOP("Cursor theme")
    }
    ListElement {
      name: QT_TR_NOOP("Face icon directory")
    }
    ListElement {
      name: QT_TR_NOOP("The files should be in username.face.icon format")
    }
    ListElement {
      name: QT_TR_NOOP("Theme directory path")
    }
    ListElement {
      name: QT_TR_NOOP("Users")
    }
    ListElement {
      name: QT_TR_NOOP("Default $PATH")
    }
    ListElement {
      name: QT_TR_NOOP("Hidden shells")
    }
    ListElement {
      name: QT_TR_NOOP("Users with these shells as their default won't be listed")
    }
    ListElement {
      name: QT_TR_NOOP("Hidden users")
    }
    ListElement {
      name: QT_TR_NOOP("Maximum user id for displayed users")
    }
    ListElement {
      name: QT_TR_NOOP("Minimum user id for displayed users")
    }
    ListElement {
      name: QT_TR_NOOP("Remember the session of the last successfully logged in user")
    }
    ListElement {
      name: QT_TR_NOOP("Remember the last successfully logged in user")
    }
    ListElement {
      name: QT_TR_NOOP("XDisplay")
    }
    ListElement {
      name: QT_TR_NOOP("Xsetup script path")
    }
    ListElement {
      name: QT_TR_NOOP("A script to execute when starting the display server")
    }
    ListElement {
      name: QT_TR_NOOP("Minimum VT")
    }
    ListElement {
      name: QT_TR_NOOP("The lowest virtual terminal number that will be used.")
    }
    ListElement {
      name: QT_TR_NOOP("X server path")
    }
    ListElement {
      name: QT_TR_NOOP("Xsession script path")
    }
    ListElement {
      name: QT_TR_NOOP("A script to execute when starting the desktop session")
    }
    ListElement {
      name: QT_TR_NOOP("Session description directory")
    }
    ListElement {
      name: QT_TR_NOOP("Xauth path")
    }
}
ListModel {
    ListElement {
      name: QT_TR_NOOP("Autologin")
    }
    ListElement {
      name: QT_TR_NOOP("Whether sddm should automatically log back into sessions when they exit")
    }
    ListElement {
      name: QT_TR_NOOP("Name of session file for autologin session (if empty try last logged in)")
    }
    ListElement {
      name: QT_TR_NOOP("Username for autologin session")
    }
    ListElement {
      name: QT_TR_NOOP("General")
    }
    ListElement {
      name: QT_TR_NOOP("Enable Qt's automatic high-DPI scaling")
    }
    ListElement {
      name: QT_TR_NOOP("Halt command")
    }
    ListElement {
      name: QT_TR_NOOP("Input method module")
    }
    ListElement {
      name: QT_TR_NOOP("Initial NumLock state. Can be on, off or none.")
    }
    ListElement {
      name: QT_TR_NOOP("If property is set to none, numlock won't be changed
NOTE: Currently ignored if autologin is enabled.")
    }
    ListElement {
      name: QT_TR_NOOP("Reboot command")
    }
    ListElement {
      name: QT_TR_NOOP("Theme")
    }
    ListElement {
      name: QT_TR_NOOP("Current theme name")
    }
    ListElement {
      name: QT_TR_NOOP("Cursor theme used in the greeter")
    }
    ListElement {
      name: QT_TR_NOOP("Number of users to use as threshold")
    }
    ListElement {
      name: QT_TR_NOOP("above which avatars are disabled
unless explicitly enabled with EnableAvatars")
    }
    ListElement {
      name: QT_TR_NOOP("Enable display of custom user avatars")
    }
    ListElement {
      name: QT_TR_NOOP("Global directory for user avatars")
    }
    ListElement {
      name: QT_TR_NOOP("The files should be named <username>.face.icon")
    }
    ListElement {
      name: QT_TR_NOOP("Theme directory path")
    }
    ListElement {
      name: QT_TR_NOOP("Users")
    }
    ListElement {
      name: QT_TR_NOOP("Default $PATH for logged in users")
    }
    ListElement {
      name: QT_TR_NOOP("Comma-separated list of shells.")
    }
    ListElement {
      name: QT_TR_NOOP("Users with these shells as their default won't be listed")
    }
    ListElement {
      name: QT_TR_NOOP("Comma-separated list of users that should not be listed")
    }
    ListElement {
      name: QT_TR_NOOP("Maximum user id for displayed users")
    }
    ListElement {
      name: QT_TR_NOOP("Minimum user id for displayed users")
    }
    ListElement {
      name: QT_TR_NOOP("Remember the session of the last successfully logged in user")
    }
    ListElement {
      name: QT_TR_NOOP("Remember the last successfully logged in user")
    }
    ListElement {
      name: QT_TR_NOOP("Wayland")
    }
    ListElement {
      name: QT_TR_NOOP("Path to a script to execute when starting the desktop session")
    }
    ListElement {
      name: QT_TR_NOOP("Directory containing available Wayland sessions")
    }
    ListElement {
      name: QT_TR_NOOP("Path to the user session log file")
    }
    ListElement {
      name: QT_TR_NOOP("X11")
    }
    ListElement {
      name: QT_TR_NOOP("Path to a script to execute when starting the display server")
    }
    ListElement {
      name: QT_TR_NOOP("Path to a script to execute when stopping the display server")
    }
    ListElement {
      name: QT_TR_NOOP("The lowest virtual terminal number that will be used.")
    }
    ListElement {
      name: QT_TR_NOOP("Arguments passed to the X server invocation")
    }
    ListElement {
      name: QT_TR_NOOP("Path to X server binary")
    }
    ListElement {
      name: QT_TR_NOOP("Path to a script to execute when starting the desktop session")
    }
    ListElement {
      name: QT_TR_NOOP("Directory containing available X sessions")
    }
    ListElement {
      name: QT_TR_NOOP("Path to the user session log file")
    }
    ListElement {
      name: QT_TR_NOOP("Path to the Xauthority file")
    }
    ListElement {
      name: QT_TR_NOOP("Path to xauth binary")
    }
    ListElement {
      name: QT_TR_NOOP("Path to Xephyr binary")
    }
}